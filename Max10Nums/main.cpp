#include <iostream>

using namespace std;

int main()
{
    int num,temp;
    cin>>num;
    temp=num;
    cin>>num;
    if(num>temp)
        temp=num;
    cin>>num;
    if(num>temp)
        temp=num;
    cin>>num;
    if(num>temp)
        temp=num;
    cin>>num;
    if(num>temp)
        temp=num;
    cin>>num;
    if(num>temp)
        temp=num;
    cin>>num;
    if(num>temp)
        temp=num;
    cin>>num;
    if(num>temp)
        temp=num;
    cin>>num;
    if(num>temp)
        temp=num;
    cin>>num;
    if(num>temp)
        temp=num;
    cout<<temp;
    return 0;
}
